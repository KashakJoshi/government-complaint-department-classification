project:
  name: Government Complaint Department Classification
  version: 1.0.0
  author: Kashak Joshi

logging:
  log_dir: logs
  log_level: INFO

artifacts:
  root_dir: artifacts

random_seed: 42

dataset:
  file_extension: ".csv"

dataset:
  file_extension: ".csv"

  required_columns:
    - post_description
    - department_name

  optional_columns:
    - post_id
    - post_title
    - department_id
    - label